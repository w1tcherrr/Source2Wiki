---
slug: /
sidebar_position: 0
hide_title: true
hide_table_of_contents: true
title: Welcome
---
import React from 'react';
import ThemedImage from '@theme/ThemedImage'
import {useHistory, useLocation} from '@docusaurus/router';

export const buttonStyle = {
    padding:0,
    border: "none",
    background: "none",
    cursor: "pointer"
};

export const gameStyle = {
    boxShadow: "0 4px 10px rgba(0,0,0,0.3)",
    borderRadius: 7,
    marginRight: 6,
    marginBottom: 3
};

export function onGameClick(game) {
    const url = new URL(window.location.origin);
    url.pathname = '/CommunityGuides/installS2Sdk';
    url.searchParams.set('game', game);
    window.location.href = url.toString();
}

# Source2 Wiki
### Community driven Documentation for everything Source2

Click on the Game Cards below to get to get started with the workshop tools.

<div style={{display: 'flex', maxWidth: '70rem'}}>
    <p>
        <button style={buttonStyle} onClick={() => onGameClick("cs2")}>
            <img className="no-zoom" style={gameStyle} src={require('@site/static/img/games/cs2_header.jpg').default} alt="Cs2 Header Image"/>
        </button>
        <button style={buttonStyle} onClick={() => onGameClick("hla")}>
            <img className="no-zoom" style={gameStyle} src={require('@site/static/img/games/hla_header.jpg').default} alt="Half-Life: Alyx Header Image"/>
        </button>
        <button style={buttonStyle} onClick={() => onGameClick("dota2")}>
            <img className="no-zoom" style={gameStyle} src={require('@site/static/img/games/dota2_header.jpg').default} alt="Dota2 Header Image"/>
        </button>
        <button style={buttonStyle} onClick={() => onGameClick("steamvr")}>
            <img className="no-zoom" style={gameStyle} src={require('@site/static/img/games/steamvr_header.jpg').default} alt="SteamVR Header Image"/>
        </button>
    </p>
</div>

:::info
Pages can be split by game and selected site-wide with the Game Selector at the top, or directly on the page if it contains multiple versions
<ThemedImage
    alt="In-Page Game Selector"
    style={{borderRadius:7}}
    sources={{
        light: '/img/wiki/game-selector-light.png',
        dark: '/img/wiki/game-selector-dark.png',
    }}
/>
:::